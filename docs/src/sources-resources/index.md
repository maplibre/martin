# Supporting Resources

Data alone is not enough to show an interactive map.
A map requires data, but it also requires fonts, sprites (sometimes called glyphs) and (dependent on the rendering engine) a style.
